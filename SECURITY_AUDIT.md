# تدقيق استقلال المصدر والأسرار

**تاريخ التدقيق:** 18 أغسطس 2026  
**النطاق:** ملفات المصدر النصية في `src/` و`server/` و`desktop/`، وملفات إعداد التوزيع داخل `standalone/`.

## النتيجة

لم يُعثر في ملفات المصدر الخادمي أو الواجهة أو موصل سطح المكتب على مراجع تشغيلية لمنصات خاصة، أو عناوين CDN، أو روابط HTTP(S) خارجية. يعتمد التنفيذ على Node.js وExpress وReact وMySQL ومكتبات منشورة في npm، مع اتصال MikroTik محلي يبدأ يدوياً من تطبيق سطح المكتب أو Android فقط.

| الفحص | النتيجة | المعالجة |
| --- | --- | --- |
| تبعيات خاصة بمنصة | لا توجد في المصدر المستقل | لا توجد حزم أو استدعاءات أو متغيرات تشغيل خاصة بمنصة. |
| روابط HTTP(S) في كود التطبيق | لا توجد في المصدر النصي | روابط التوثيق المحلية فقط مثل `localhost` لا تمثل اعتماداً خارجياً. |
| أسرار مضمنة | لا توجد | `.gitignore` يستبعد ملفات البيئة والمفاتيح والشهادات وحزم البناء. |
| كلمات مرور أجهزة MikroTik | مشفرة عند التخزين | يستخدم الخادم AES-256-GCM ومفتاح `RESOURCE_ENCRYPTION_KEY` خارج المصدر. |
| كلمات مرور الحسابات | لا تخزن بصيغة قابلة للاسترجاع | يستخدم الخادم bcrypt مع تكلفة 12. |
| الجلسات | محمية | جلسة JWT موقعة في Cookie من نوع HTTP-only وSameSite=Lax. |

## المتغيرات المطلوب حمايتها

يجب إدخال القيم الآتية في واجهة أسرار الاستضافة أو في ملف محلي غير مرفوع باسم `.env`: `DATABASE_URL` أو متغيرات MySQL الخاصة بـCompose، و`JWT_SECRET`، و`RESOURCE_ENCRYPTION_KEY`، وبيانات المدير الأولي. يشرح ملف `ENVIRONMENT.example` الأسماء فقط من دون قيم قابلة للاستخدام.

> لا تنشئ ملفاً يحوي قيماً فعلية ضمن المستودع العام. يجب تنفيذ الترحيل الأول في بيئة موثوقة، ثم حفظ بيانات المدير الأولي خارج GitHub.

## قيود التحقق

تعذر بناء حاوية Docker في بيئة التطوير الحالية لأن برنامج Docker غير متاح فيها. تمت مراجعة Dockerfile وCompose من حيث البنية، بينما تم التحقق فعلياً من فحص الأنواع والاختبارات وبناء PWA وتغليف Electron وبناء APK.
