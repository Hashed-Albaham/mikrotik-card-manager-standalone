# البنية المستقلة لمدير كروت MikroTik

## المبدأ

النسخة المستقلة لا تستخدم مصادقة أو تخزيناً أو تحليلات أو واجهات خدمة خاصة بأي منصة. تعتمد على ملفات التطبيق نفسها، وبيانات محلية مشفرة، وحزم مفتوحة المصدر من السجل العام فقط.

| الهدف | التقنية المستقلة | وضع الاتصال |
| --- | --- | --- |
| تطبيق الويب المثبّت | React + Vite + Service Worker | يعمل دون إنترنت للطباعة والتوليد والتصدير |
| التخزين المحلي | IndexedDB + Web Crypto | محلي ومشفر عند تفعيل قفل التطبيق |
| PDF | pdf-lib | يُنشأ محلياً من دون نافذة طباعة أو طلب شبكة |
| سطح المكتب | Electron + Node.js | اتصال API الثنائي وREST على شبكة الراوتر المحلية |
| Android APK | Capacitor + مكوّن Android TCP محلي | REST لـRouterOS 7 وAPI الثنائية لـRouterOS 6/7 |
| التشغيل الخادمي الاختياري | Node.js + MySQL أو SQLite | ذاتي الاستضافة فقط |

## وضع عدم الاتصال

يعمل التطبيق دون شبكة لإنشاء الباقات، ومنع التكرار المحلي، وحفظ القوالب والخلفيات، وتوليد RSC وTXT وPDF. لا يوجد إرسال تلقائي إلى MikroTik في أي وضع.

> الاتصال المباشر عبر منفذ API الثنائي غير متاح من متصفح PWA المجرد، لأن المتصفح لا يوفر TCP خاماً. لذلك تكون وظيفة إرسال RouterOS 6/7 المباشر متاحة في نسخة سطح المكتب وAPK عبر طبقة أصلية، بينما يبقى PWA قادراً على تنزيل RSC للتنفيذ اليدوي.

## تدفق الدفعة

1. ينشئ المستخدم الدفعة محلياً أو يربطها بجهاز محفوظ.
2. يحفظ التطبيق PDF وRSC وTXT والبيانات محلياً قبل أي اتصال.
3. يمكن حذف الدفعة أو طباعتها أو تنزيل سكربتها بلا أي أثر على الراوتر.
4. لا يُتاح الاتصال إلا بزر **إرسال الدفعة إلى MikroTik** وبعد تأكيد صريح.
5. عند الإرسال يحفظ التطبيق سكربتاً واحداً باسم الدفعة على الجهاز ثم يشغله مرة واحدة.

## مراجع القرار

يوفر Vite PWA ملف Manifest وعامل خدمة لتخزين الأصول وتفعيل الاستخدام دون شبكة [1]. ويوثق Capacitor أن أصول الويب المبنية تُنسخ إلى مشروع Android الأصلي [2]. ويشغل Electron JavaScript وHTML وCSS داخل تطبيقات سطح المكتب مع Node.js المضمن [3].

## المراجع

[1]: https://vite-pwa-org.netlify.app/guide/ "Vite PWA — Getting Started"
[2]: https://capacitorjs.com/solution/react "Capacitor — React"
[3]: https://www.electronjs.org/docs/latest/ "Electron Documentation"
